import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    align-items: center;
    gap: 10px;
`;

export const Title = styled.span`
    font-size: 1.7rem;
    font-weight: 500;
    position: relative;
`;

export const Decocation = styled.span`
    height: 2px;
    width: 100px;
    background: linear-gradient(to right, #000, #999); 
`;